import { EXAMPLE_PATH } from '../../lib/constants'

const Footer = () => {
  return (
    <footer className="bg-primaryBackground">
      <div className="py-28 flex flex-col lg:flex-row items-center">Footer</div>
    </footer>
  )
}

export default Footer
